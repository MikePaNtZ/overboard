# Docs

The Notion workspace is the **primary home** for Overboard's vision, design,
and roadmap docs. This directory holds Markdown+Mermaid mirrors that track
the implementation as it lands; Notion may drift ahead during heavy dev, and
a periodic cleanup pass reconciles the two. See the root `CLAUDE.md` for the
iteration loop.
