[package]
name = "control-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
board-types = { workspace = true }
# no_std trig: control-core cannot use std::f32::atan2.
libm = "0.2"
